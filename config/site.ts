export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "OpenxAI Genesis",
  description:
    "The OpenxAI Genesis is an exclusive invite for 50 early backers to join our mission, securing a discounted $OPENX allocation.",
} as const
